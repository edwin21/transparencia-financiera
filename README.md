# Portal transparencia financiera

## Requisitos

- .Net 6
- C# 10
- Asegurate que `export PATH=$HOME/.dotnet/tools:$PATH` esta en tu path

## Dev

- `dotnet restore`
- `dotnet build`
- `dotnet ef database update`
